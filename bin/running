#!/usr/bin/env bash
set -euo pipefail
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND" >&2' ERR

# Mark the current tmux pane as running. Call this from inside a pane when the
# process starts to work again.
#
# Usage: running

exec pane-state running

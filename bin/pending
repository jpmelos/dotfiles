#!/usr/bin/env bash
set -euo pipefail
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND" >&2' ERR

# Mark the current tmux pane as pending. Call this from inside a pane when the
# process waits for input from the user.
#
# Usage: pending

exec pane-state pending

#!/usr/bin/env bash
set -euo pipefail
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND" >&2' ERR

# This is just to allow AI agents to rename files. Since they always run inside
# a Docker container and they only have access to things that are under version
# control, this is fine.

exec mv "$@"

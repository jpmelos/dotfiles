#!/usr/bin/env bash
set -euo pipefail
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND" >&2' ERR

# Mark the current tmux pane as ready. Call this from inside a pane when the
# process did its work and can receive more instructions.
#
# Usage: ready

exec pane-state ready

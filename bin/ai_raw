#!/usr/bin/env bash
set -euo pipefail
trap 'echo "Exit status $? at line $LINENO from: $BASH_COMMAND" >&2' ERR

if [[ "${AGENT_ENV_SET:-}" != "true" ]]; then
    echo "Error: run 'eval \$(ai --env)' to prepare the environment" >&2
    exit 1
fi

ai --no-rebuild -- "$@"
